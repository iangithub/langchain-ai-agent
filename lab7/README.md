# Lab7: 使用 Streamable HTTP 模式串接 MCP Server

## 概述

這是一個展示如何使用 **Model Context Protocol (MCP)** 的範例，透過 **Streamable HTTP** 傳輸模式連接 MCP Server，讓 Agent 系統可以使用遠端工具。

以「客戶服務系統」為應用場景，提供產品資訊查詢和訂單狀態查詢功能。

## 什麼是 MCP (Model Context Protocol)？

MCP 是由 Anthropic 開發的開放標準，允許 AI 應用程式以標準化的方式連接到外部工具和資料源。

### MCP 的優勢

- **標準化**：統一的工具定義和呼叫方式
- **可擴展**：輕鬆添加新的工具和服務
- **跨平台**：支援多種 AI 框架（Claude、LangChain、OpenAI 等）
- **安全**：支援認證和授權機制

### Streamable HTTP 傳輸模式

Streamable HTTP 是 MCP 推薦的生產環境傳輸模式（2025-03-26 引入），取代了舊版的 HTTP+SSE 模式。

特點：
- 適合遠端部署
- 支援負載平衡
- 可擴展性佳
- 無狀態設計

## 系統架構

```
┌─────────────────────────────────────────────────────────────────┐
│                      Lab7 系統架構                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                    Agent 系統                             │   │
│  │               (lab7_agent_mcp_streaming.py)              │   │
│  │                                                          │   │
│  │  ┌─────────────────┐    ┌─────────────────────────────┐  │   │
│  │  │   LangChain     │    │  MultiServerMCPClient       │  │   │
│  │  │   create_agent  │◄──►│  (langchain-mcp-adapters)   │  │   │
│  │  └─────────────────┘    └──────────────┬──────────────┘  │   │
│  │                                        │                  │   │
│  └────────────────────────────────────────┼──────────────────┘   │
│                                           │                      │
│                         Streamable HTTP   │                      │
│                    http://localhost:8000/mcp                     │
│                                           │                      │
│  ┌────────────────────────────────────────┼──────────────────┐   │
│  │                    MCP Server          ▼                  │   │
│  │                 (mcp_server.py)                           │   │
│  │                                                           │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────────┐   │   │
│  │  │get_product_ │  │get_order_   │  │list_available_  │   │   │
│  │  │info         │  │status       │  │products         │   │   │
│  │  └──────┬──────┘  └──────┬──────┘  └────────┬────────┘   │   │
│  │         │                │                  │            │   │
│  │         ▼                ▼                  ▼            │   │
│  │  ┌─────────────────────────────────────────────────────┐ │   │
│  │  │              模擬資料庫                              │ │   │
│  │  │    PRODUCTS_DATABASE    ORDERS_DATABASE             │ │   │
│  │  └─────────────────────────────────────────────────────┘ │   │
│  │                                                           │   │
│  └───────────────────────────────────────────────────────────┘   │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

## MCP Server 提供的工具

| 工具名稱                | 功能                         |
| ----------------------- | ---------------------------- |
| get_product_info        | 根據產品名稱查詢產品詳細資訊 |
| get_order_status        | 根據訂單號碼查詢訂單狀態     |
| list_available_products | 列出所有可查詢的產品         |
| list_sample_orders      | 列出範例訂單號碼供測試       |

## 核心概念

### 1. MCP Server 實作

使用 FastMCP 建立 MCP Server：

```python
from mcp.server.fastmcp import FastMCP

# 建立 FastMCP 實例
mcp = FastMCP(
    name="產品與訂單查詢服務",
    json_response=True
)

# 定義工具
@mcp.tool()
def get_product_info(product_name: str) -> str:
    """根據產品名稱查詢產品的詳細資訊。"""
    # 實作查詢邏輯...
    return result

# 啟動 Streamable HTTP 服務
mcp.run(
    transport="streamable-http",
    host="0.0.0.0",
    port=8000
)
```

### 2. Agent 連接 MCP Server

使用 langchain-mcp-adapters 連接 MCP Server：

```python
from langchain_mcp_adapters.client import MultiServerMCPClient
from langchain.agents import create_agent

# 設定 MCP Server 連線
MCP_SERVER_CONFIG = {
    "product_order_service": {
        "url": "http://localhost:8000/mcp",
        "transport": "streamable_http",
    }
}

# 建立 MCP 客戶端並取得工具
mcp_client = MultiServerMCPClient(MCP_SERVER_CONFIG)
tools = await mcp_client.get_tools()

# 使用工具建立 Agent
agent = create_agent(
    model="gpt-4o",
    tools=tools,
    system_prompt="你是一位客戶服務助理..."
)
```

### 3. 工具自動轉換

langchain-mcp-adapters 會自動將 MCP 工具轉換為 LangChain 相容格式：

- MCP 工具定義 → LangChain Tool
- 參數驗證自動處理
- 工具呼叫結果自動解析

## 使用技術

- **MCP Python SDK**: Anthropic 官方 MCP 實作
- **FastMCP**: 簡化 MCP Server 開發
- **langchain-mcp-adapters**: LangChain MCP 整合
- **Streamable HTTP**: MCP 推薦傳輸模式
- **LangChain create_agent**: Agent 建立

## 安裝與設定

### 1. 安裝依賴

```bash
pip install -r requirements.txt
```

需要額外安裝的套件：
- `mcp`: Anthropic MCP Python SDK
- `langchain-mcp-adapters`: LangChain MCP 整合

### 2. 設定環境變數

```bash
cp .env.example .env
# 編輯 .env 填入 OPENAI_API_KEY
```

### 3. 啟動 MCP Server

```bash
python mcpserver/mcp_server.py
```

輸出：
```
============================================================
MCP Server - 產品與訂單查詢服務
============================================================

使用 Streamable HTTP 傳輸模式
服務位置：http://localhost:8000/mcp

可用工具：
  - get_product_info: 查詢產品資訊
  - get_order_status: 查詢訂單狀態
  - list_available_products: 列出所有產品
  - list_sample_orders: 列出範例訂單

按 Ctrl+C 停止服務
============================================================
```

### 4. 執行 Agent 系統

開啟新的終端機：

```bash
python lab7/lab7_agent_mcp_streaming.py
```

## 程式碼結構

```
langchain-ai-agent/
├── mcpserver/
│   └── mcp_server.py           # MCP Server 實作
├── lab7/
│   ├── lab7_agent_mcp_streaming.py  # Agent 系統
│   └── README.md                     # 說明文件
└── requirements.txt             # 依賴套件
```

### 主要組件

**MCP Server (mcp_server.py)**：
- `PRODUCTS_DATABASE`: 模擬產品資料庫
- `ORDERS_DATABASE`: 模擬訂單資料庫
- `get_product_info()`: 產品查詢工具
- `get_order_status()`: 訂單查詢工具

**Agent 系統 (lab7_agent_mcp_streaming.py)**：
- `create_mcp_agent()`: 建立連接 MCP 的 Agent
- `stream_chat()`: 串流對話處理
- `run_demo()`: 展示模式
- `run_interactive()`: 互動模式

## 輸出範例

```
============================================================
Lab7: Agent 透過 Streamable HTTP 連接 MCP Server
============================================================

✓ 環境變數載入成功

[資訊] MCP Server 位置: http://localhost:8000/mcp
[提示] 請確認已啟動 MCP Server (python mcpserver/mcp_server.py)

============================================================
展示：Agent 使用 MCP 工具回應查詢
============================================================

[系統] 正在連接 MCP Server...
[系統] 成功連接 MCP Server
[資訊] 載入的工具：
  - get_product_info: 根據產品名稱查詢產品的詳細資訊。...
  - get_order_status: 根據訂單號碼查詢訂單的當前狀態。...
  - list_available_products: 列出所有可查詢的產品清單。...
  - list_sample_orders: 列出範例訂單號碼供查詢測試。...

[使用者]: 請幫我查詢 AirPure Pro 空氣清淨機的產品資訊
------------------------------------------------------------
[助理]:
[工具呼叫: get_product_info]

根據查詢結果，以下是 AirPure Pro 智能空氣清淨機的詳細資訊：

【產品資訊】
- 產品名稱：AirPure Pro 智能空氣清淨機
- 產品類別：家電
- 價格：NT$ 12,800
- 庫存狀態：有庫存（156 件）

產品描述：
專為現代家庭設計的新一代智能空氣清淨機，配備 HEPA-13 濾網，
可捕捉 99.97% 的空氣微粒，包括灰塵、花粉和寵物皮屑。

產品特色：
- HEPA-13 級濾網
- 智能空氣品質偵測 (PM2.5, VOC)
- App 遠端控制
- 超靜音設計 (25dB)
- 適用 30 坪空間

保固期間：2年保固

============================================================

[使用者]: 我想查詢訂單 ORD-2024-002 的配送狀態
------------------------------------------------------------
[助理]:
[工具呼叫: get_order_status]

您的訂單 ORD-2024-002 目前的狀態如下：

【訂單狀態查詢】
- 訂單號碼：ORD-2024-002
- 訂單狀態：🚚 運送中

訂購商品：SmartWatch X1 智能手錶
數量：2 件
訂單金額：NT$ 17,980

訂購日期：2024-12-27
出貨日期：2024-12-28
預計送達：2024-12-30
物流編號：TW987654321
配送地址：新北市板橋區中山路 50 號

您的訂單正在運送途中，請留意收件！

============================================================
互動模式（輸入 'quit' 結束）
============================================================
```

## 測試資料

### 可查詢的產品

| 產品名稱               | 價格        | 類別       |
| ---------------------- | ----------- | ---------- |
| AirPure Pro 智能空氣清淨機 | NT$ 12,800 | 家電       |
| SmartWatch X1 智能手錶    | NT$ 8,990  | 穿戴裝置   |
| SoundPods Ultra 真無線藍牙耳機 | NT$ 3,990 | 音訊設備   |
| ProBook 15 輕薄筆電       | NT$ 42,900 | 電腦       |

### 可查詢的訂單

| 訂單號碼      | 狀態   | 商品                     |
| ------------- | ------ | ------------------------ |
| ORD-2024-001  | 已送達 | AirPure Pro 智能空氣清淨機 |
| ORD-2024-002  | 運送中 | SmartWatch X1 智能手錶    |
| ORD-2024-003  | 處理中 | SoundPods Ultra 真無線藍牙耳機 |
| ORD-2024-004  | 已取消 | ProBook 15 輕薄筆電       |

## 學習重點

1. **MCP 協定**：了解 Model Context Protocol 的設計理念
2. **Streamable HTTP**：掌握現代 MCP 傳輸模式
3. **FastMCP**：學習快速建立 MCP Server
4. **LangChain 整合**：了解如何將 MCP 工具與 LangChain Agent 結合

## 與其他 Lab 的比較

| 項目       | 其他 Lab           | Lab7 (MCP)              |
| ---------- | ------------------ | ----------------------- |
| 工具來源   | 本地定義           | 遠端 MCP Server         |
| 工具部署   | 與 Agent 同進程    | 獨立服務，可分散部署    |
| 可擴展性   | 需修改程式碼       | 動態載入新工具          |
| 適用場景   | 單一應用           | 多應用共享工具          |

## 延伸學習

- **多 MCP Server**：連接多個 MCP Server
- **認證機制**：為 MCP Server 加入 OAuth 認證
- **工具組合**：結合本地工具與 MCP 工具
- **生產部署**：使用 Docker 部署 MCP Server
